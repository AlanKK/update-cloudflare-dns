{
    "pushbullet-api-key": "Leave empty for no notification",
    "cf-api-key": "Cloudflare API Key",
    "cf-update-target": [
        {
            "name": "hostname.something.com",
            "id": "dns record id. Only found in audit log records",
            "zone-id": ""
        }
    ]
}